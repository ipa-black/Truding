name: Build Diamond Trade IPA

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build_ios:
    name: Build iOS App
    runs-on: macos-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'

      # التعديل هنا: إجبار فلاتر على استخدام اسم بحروف صغيرة لتجنب خطأ الحرف الكبير
      - name: Create Flutter Project Structure
        run: flutter create --project-name diamond_trade .

      - name: Install Dependencies
        run: flutter pub get

      - name: Set App Name
        run: |
          /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName Diamond Trade" ios/Runner/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleName DiamondTrade" ios/Runner/Info.plist

      - name: Build iOS (No Codesign)
        run: flutter build ios --release --no-codesign

      - name: Convert to IPA
        run: |
          mkdir -p Payload
          mv build/ios/iphoneos/Runner.app Payload/
          zip -qq -r DiamondTrade.ipa Payload

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: DiamondTrade-App
          path: DiamondTrade.ipa
